---
const { title, url, image, date, author, description } = Astro.props
---
<div class="w-1/2 rounded-lg bg-base-100 p-5 transition ease-in-out delay-150 duration-300 hover:scale-105 mx-auto my-5"><a href={url}><img src={image.url} alt={image.alt} class=" mx-auto rounded-xl" transition:name={"BlogPost" + url}/>{title}<span class=" float-end">{author}</span><br />{String(date).slice(0, 15)}<br /><span>{description}</span></a></div>